name: 🧪 QA Verification
description: Verify a feature, fix, or release in staging or production
title: "[QA]: Verify [Feature/Issue #]"
labels: [qa, testing]
body:
  - type: markdown
    attributes:
      value: |
        Please complete this QA verification template for the given feature, fix, or release.

  - type: input
    id: linked_issue
    attributes:
      label: 🔗 Related Issue or PR
      description: Link the issue or pull request under test.
      placeholder: e.g. #42 or PR #123
    validations:
      required: true

  - type: dropdown
    id: environment
    attributes:
      label: 🌍 Environment Tested
      options:
        - Staging
        - Production
        - Local
    validations:
      required: true

  - type: textarea
    id: scope
    attributes:
      label: 🧭 Test Scope
      description: What parts of the app are being verified?
      placeholder: e.g. "Verify login, user profile update, and logout flow."
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: ✅ Steps Tested
      description: Describe how you tested.
      placeholder: |
        1. Log in as a normal user
        2. Update profile
        3. Refresh the page
        4. Verify persistence
    validations:
      required: true

  - type: dropdown
    id: result
    attributes:
      label: 🧩 Test Result
      options:
        - Pass ✅
        - Fail ❌
        - Blocked 🚧
    validations:
      required: true

  - type: textarea
    id: notes
    attributes:
      label: 🗒️ Additional Notes / Screenshots
      description: Add screenshots or notes for failures or edge cases.
      placeholder: "Attach images or logs if relevant."
